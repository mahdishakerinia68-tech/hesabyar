# حساب‌یار pro1.4

## مشکل ۱: جابه‌جا شدن تاریخ تراکنش‌ها
لیست تراکنش‌ها (هم «اخیر» در داشبورد، هم صفحه‌ی «تراکنش‌ها»، هم جستجوی سراسری و
هم لیست انتقال‌ها) بر اساس ترتیب افزوده‌شدن به آرایه نمایش داده می‌شد، نه بر
اساس تاریخ. وقتی یک تراکنش تکرارشونده برای تاریخی در گذشته ساخته می‌شد یا بین
دو گوشی همگام‌سازی می‌شد، ترتیب افزودن با ترتیب تاریخ یکی نبود و تراکنش‌ها در
لیست نامرتب/تاریخ‌ها جابه‌جا به‌نظر می‌رسیدند.
**تغییر:** همه‌ی این لیست‌ها اکنون همیشه بر اساس تاریخ (نزولی، جدیدترین بالا)
مرتب می‌شوند.

همچنین در بازسازی برنامه‌ی اقساط یک بدهکار/بستانکار (وقتی فقط مبلغ کل یا
تاریخ سررسید تغییر می‌کرد)، تاریخ شروع با `new Date()` روی یک رشته‌ی تاریخِ
بدون ساعت خوانده می‌شد که با بقیه‌ی برنامه (که از یک تابع امن‌تر برای همین کار
استفاده می‌کند) ناهماهنگ بود و می‌توانست باعث یک‌روز جابه‌جایی در تاریخ سررسید
اقساط بازسازی‌شده شود؛ اصلاح شد. همین ناهماهنگی در محاسبه‌ی سررسید چک‌ها،
مرتب‌سازی چک‌ها و مرتب‌سازی فاکتورهای مشتری هم یکدست شد.

## مشکل ۲: تیک‌خوردن آیتم یادداشت در جدول هفتگی مخفی نمی‌شد
وقتی همه‌ی آیتم‌های زیرمجموعه‌ی یک یادداشت تیک می‌خوردند (انجام می‌شدند)،
یادداشت همچنان در «جدول هفتگی» نمایش داده می‌شد، انگار هنوز کاری باقی مانده.
**تغییر:** حالا اگر یادداشت حداقل یک آیتم داشته باشد و همه‌ی آیتم‌هایش
انجام‌شده باشند، از جدول هفتگی همان روز مخفی می‌شود (دقیقاً مثل یادآوری که با
✓ از لیست هفتگی حذف می‌شود). یادداشت‌های بدون آیتم (فقط متنی) طبق قبل همیشه
نمایش داده می‌شوند.

نسخه از pro1.3 به pro1.4 تغییر کرد (index.html, manifest.json, app.js, sw.js,
package.json، ماژول‌های core/security و فایل‌های تست).

## pro1.4.1 — تکمیل رفع باگ جدول هفتگیِ یادداشت‌ها
رفع قبلی فقط وقتی «همه‌ی» آیتم‌های یک یادداشت تیک می‌خوردند کل یادداشت را از
جدول هفتگی پنهان می‌کرد. اگر یادداشت چند آیتم داشت و فقط یکی از آن‌ها تیک
می‌خورد، یادداشت (با همان عنوان کلی) همچنان نمایش داده می‌شد — دقیقاً همان
چیزی که گزارش شد: «تیک می‌خورد، می‌مونه، مخفی نمی‌شه».
**تغییر:** برای یادداشت‌هایی که چند آیتم دارند، به‌جای یک ردیفِ کلی برای کل
یادداشت، هر آیتمِ هنوز انجام‌نشده جداگانه در جدول هفتگی نمایش داده می‌شود. به‌
محض تیک‌خوردنِ همان آیتم، دقیقاً همان ردیف از جدول پنهان می‌شود، و وقتی همه‌ی
آیتم‌ها تیک بخورند، یادداشت کامل از آن روز حذف می‌شود.
