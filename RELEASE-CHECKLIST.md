# چک‌لیست انتشار (کوتاه؛ هر مرحله زیر ۹۰ ثانیه)

## قبل از ساخت zip
- [ ] `npm run validate` سبز
- [ ] `npm test` سبز
- [ ] `npm run test:browser` سبز (۱۷ از ۱۷)
- [ ] یک برچسب نسخه در همه‌جا (تست `tests/run.mjs` این را چک می‌کند)
- [ ] بسته شامل `node_modules`، `*.zip`، `.env` یا کلید API نیست (`security-check`)

## قبل از انتشار روی HTTPS
- [ ] قوانین Firestore (`firestore.rules`) روی پروژه‌ی Firebase deploy شده‌اند
- [ ] روی یک گوشی واقعی: نصب PWA، ثبت یک تراکنش، بستن کامل برنامه، بازکردن دوباره
- [ ] روی همان گوشی: حالت هواپیما ← برنامه باز می‌شود و داده‌ها هستند
- [ ] یک پشتیبان بگیر، داده را پاک کن و بازیابی کن

## بعد از انتشار
- [ ] نسخه‌ی نمایش‌داده‌شده در تنظیمات با برچسب انتشار یکی است
