# حساب‌یار pro1 — رفع باگ «تراکنش‌های ناپدید»

## مشکل گزارش‌شده
صفحه‌ی «تراکنش‌ها» گاهی یک سری از تراکنش‌ها را نشان نمی‌داد و همان تراکنش‌ها در
محاسبه‌ی «مانده حساب» هم به‌حساب نمی‌آمدند.

## علت
`render()` لیست تراکنش‌ها را با یک `Array.map(txHTML).join("")` در یک عبارت
واحد می‌ساخت. اگر یک رکورد قدیمی/ناقص (مثلاً باقی‌مانده از نسخه‌های قبلی یا
یک همگام‌سازی نیمه‌کاره) باعث خطا در `txHTML` می‌شد، کل عبارت شکست می‌خورد و
`innerHTML` هرگز به‌روزرسانی نمی‌شد — یعنی لیست همان محتوای قبلی (بدون
تراکنش‌های تازه) را نشان می‌داد، بدون هیچ خطای قابل مشاهده برای کاربر.

## تغییرات
- هر ردیف تراکنش (و هر ردیف انتقال) اکنون جدا از بقیه رندر می‌شود
  (`safeTxHTML` / try-catch دور `transferItemHTML`)؛ یک رکورد خراب فقط خودش
  نادیده گرفته می‌شود (با هشدار در کنسول) و بقیه‌ی تراکنش‌ها طبق معمول
  نمایش داده می‌شوند.
- `accountBalance()` هم اکنون هر تراکنش را جدا محاسبه می‌کند تا یک رکورد
  خراب کل محاسبه‌ی مانده حساب را به هم نریزد.
- فیلتر دسته‌بندی در صفحه‌ی تراکنش‌ها اصلاح شد: انتخاب یک دسته‌ی والد
  (مثلاً «خوراک») حالا زیردسته‌های آن (مثلاً «خوراک - رستوران») را هم نشان
  می‌دهد؛ قبلاً فقط تراکنش‌هایی با نام دسته‌ی دقیقاً برابر نشان داده می‌شدند
  و زیردسته‌ها از فیلتر جا می‌ماندند.
- برچسب نسخه در همه‌جا (PWA، منیفست، سرویس‌ورکر، تنظیمات) به `pro1` تغییر
  کرد.

## نکته
اگر بعد از نصب این نسخه باز هم موردی از تراکنش‌های ناپدید دیدید، لطفاً از
تنظیمات یک پشتیبان (Backup) JSON بگیرید و بفرستید — چون خطای احتمالی حالا
در کنسول مرورگر (`console.warn`) هم ثبت می‌شود و پیدا کردن رکورد دقیق مقصر
را آسان‌تر می‌کند.
