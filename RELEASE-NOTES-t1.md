# حساب‌یار t1 — اصلاح ذخیره‌سازی و پایدارسازی

## خطاهای مهمِ 1.2.8 که رفع شدند
- `storageWriteQueue` تعریف نشده بود؛ در نتیجه هر ذخیره پیام «حافظه پر است» می‌داد، صفحه دوباره رسم نمی‌شد و هیچ داده‌ای در IndexedDB نوشته نمی‌شد.
- `getAutoBackupInfo` وجود نداشت؛ `initApp` وسط راه خطا می‌داد و بقیه‌ی راه‌اندازی (همگام‌سازی، یادآورها، بررسی به‌روزرسانی) اجرا نمی‌شد.
- تنظیمات (رمز ورود و الگو، زبان، برند فروشگاه، تسویه‌ی سال) در IndexedDB ذخیره نمی‌شدند و با ریلود از بین می‌رفتند.
- امکان بازنویسی داده‌ی واقعی با داده‌ی خالی قبل از پایان خواندن؛ و پاک شدن نسخه‌ی قدیمی localStorage پیش از تأیید نوشتن.
- «پشتیبان خودکار» فقط ظاهر بود (تابع خالی) ولی ادعای ساخت فایل هر ۶ ساعت می‌کرد؛ پیام حداقل رمز بکاپ «۴ کاراکتر» بود ولی شرط واقعی ۸ است.
- `sw.js` فایل `storage-runtime.js` را برای شروع آفلاین cache نمی‌کرد.
- پنجره‌ی «تغییرات نسخه» شماره‌ی نسخه‌های قدیمی را نشان می‌داد.

## تغییرات
- `src/core/storage-runtime.js` بازنویسی شد: یک transaction اتمیک برای رکوردها + تنظیمات، بازیابی از بستن اتصال، درخواست persistent storage، و بررسی خواندن‌مجدد.
- صف ذخیره‌سازی با ادغام درخواست‌ها، گزارش خطا فقط پس از شکست واقعی، و بازگشت به localStorage اگر IndexedDB نباشد.
- «پشتیبان خودکار» ← «یادآوری پشتیبان‌گیری» (۷ روز).
- فونت وزیرمتن دیگر رندر را مسدود نمی‌کند (`@import` حذف شد)؛ مهم برای شبکه‌های کند یا فیلتر؛ در نبود فونت، فونت‌های سیستم استفاده می‌شوند.
- دسترس‌پذیری: حلقه‌ی focus برای کیبورد و احترام به `prefers-reduced-motion`.
- برچسب یکسان `t1` در برنامه، PWA، manifest، cache و بسته؛ `manifest.name` از «حساب‌یار 2» به «حساب‌یار» اصلاح شد.
- پروژه‌ی web-only شد: dependencyهای Capacitor/Vitest/Playwright/ESLint از `package.json` حذف شدند (lock قبلی برای `npm ci` معتبر نبود)، workflow اندروید با CI واقعی (validate + test + browser) جایگزین شد، `src/core/storage.js` تکراری حذف شد.
- منطق فاکتور در `src/modules/invoices.js` دقیقاً با `app.js` یکی شد و تست تفاضلی روی ۵۰۰ فاکتور تصادفی اضافه شد.

## تست‌ها
تست واقعی مرورگر (۱۷ سناریو) اضافه شد؛ برای اطمینان از اثربخشی، دو خطای عمدی (حذف ذخیره‌ی تنظیمات، حذف قفل پیش از hydrate) تزریق شد و تست‌ها آن‌ها را گرفتند.
